/**
 * Bright label colors for rebuild section kicker pills (`RebuildSectionKicker`).
 * Washes use `color-mix(in srgb, accent 22%, var(--wx-page-bg))` on the chip.
 */
export const REBUILD_SECTION_KICKER_ACCENTS = /** @type {const} */ ({
  process: "#2dd4bf",
  studio: "#fb7185",
  faq: "#4ade80",
  contact: "#fb923c",
});
