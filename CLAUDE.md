# wineury.design

Canvas repo: `src/styles/tokens.css`, Motion + GSAP + Lenis as already wired.

**Merge order when skills disagree**

1. This repo: tokens, stack, `README` roadmap.
2. Taste / “anti-slop” skills: composition and restraint; keep wineury tokens and roles.
3. Lazyweb: reference real patterns when the skill’s tooling is available.
4. UX: `interaction-design`, `frontend-design`, `web-design-guidelines` (and Vercel agent skills).
5. A11y + motion safety: `fixing-accessibility`, `accessible-motion`.
6. Implementation: GSAP skills, `framer-motion` skill (use with `motion` package), Lottie skills, `vercel-react-best-practices`.

```bash
npx skills experimental_install
```
